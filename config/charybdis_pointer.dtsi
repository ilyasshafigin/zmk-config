#include <input/processors.dtsi>
#include <input/processors/zmk-input-processor-keybind.dtsi>
#include <behaviors/input_gestures_accel.dtsi>
#include <dt-bindings/zmk/input_transform.h>
#include "includes/layers.h"
#include "zmk-helpers/key-labels/36.h"

&trackball_listener {
    input-processors = <
        &zip_xy_scaler 2 3
        &pointer_accel
    >;

    slow {
        layers = <SLOW>;
        input-processors = <
            &zip_xy_scaler 1 3
            &pointer_accel
        >;
    };

    scroll {
        layers = <SCRL>;
        input-processors = <
            &zip_xy_scaler 1 40
            &pointer_accel
            &zip_xy_to_scroll_mapper
            &zip_scroll_transform (INPUT_TRANSFORM_X_INVERT | INPUT_TRANSFORM_Y_INVERT)
        >;
    };

    caret {
        layers = <CARE>;
        input-processors = <&zip_keybind_keys>;
        process-next;
    };

    // хак, чтобы на курсор не перемещался на слое CARE
    // https://github.com/zmkfirmware/zmk/issues/2967#issuecomment-2982313146
    caret-prevent-move {
        layers = <CARE>;
        input-processors = <&zip_xy_scaler 0 1>;
    };

    // временно убрал auto mouse layer

    // хак, чтобы на остальных слоях трекбол не работал
    // disable {
    //    layers = <SYM NAV EXT FN>;
    //    input-processors = <&zip_xy_scaler 0 1>;
    //};

    // mouse-game {
    //     layers = <GAM GFN>;
    //     input-processors = <&zip_xy_scaler 1 1>;
    // };

    // mouse {
    //     layers = <MSE DEF>;
    //     input-processors = <&auto_mouse_layer MSE 1000>;
    // };
};

&pointer_accel {
    input-type = <INPUT_EV_REL>;
    track-remainders;
    min-factor = <600>;
    max-factor = <2000>;
    speed-threshold = <1500>;
    speed-max = <5000>;
    acceleration-exponent = <1>;
};

/ {
    // временно убрал auto mouse layer
    // auto_mouse_layer: auto_mouse_layer {
    //     compatible = "zmk,input-processor-temp-layer";
    //     #input-processor-cells = <2>;
    //     require-prior-idle-ms = <500>;
    //     excluded-positions = <
    //         LH2 LH1 LH0     // Left Thumbs
    //         RM1 RM2 RM3 LB0 // Mouse buttons
    //         LM4 LM3 LM2 LM1 // Left Home-row mod
    //         LB3 LB2 LB1     // Left Mouse layers
    //         RB1 RB2 RB3     // Right Mouse layers
    //     >;
    // };

    zip_keybind_keys: zip_keybind_keys {
        compatible = "zmk,input-processor-keybind";
        #input-processor-cells = <0>;
        track_remainders;
        bindings = <&kp RIGHT>,
                   <&kp LEFT>,
                   <&kp UP>,
                   <&kp DOWN>;
        tick = <40>;
        wait-ms = <0>;
        tap-ms = <10>;
    };
};
